void main() {
//   // const input = 12;
//   // final output = compliment(input);
//   // print(output);

//   // helloPersonAndPet('Wilson', 'Bingo');

//   print(fullName(
//     "Osagie",
//     'Abiodun',
//   ));
//   print(withinTolerance(value: 9, min: 7, max: 11));
// }

// //First function i created today
// String compliment(int number) {
//   return '$number is a very nice number.';
// }

// //Second funciton i created today
// void helloPersonAndPet(String person, String pet) {
//   print('Hello, $person, and your furry friend, $pet!');
// }

// //
// //third fucntion
// String fullName(String first, String last, [String? title]) {
//   // if (title != null) {
//   //   return '$title $first $last';
//   // } else {
//   //   return '$first $last';
//   // }
//   return title != null ? '$title $first $last' : '$first $last';
// }

// //fourth function with naming the parameters with curly braces being added kills the positioning and also making the paremeters optional
// bool withinTolerance({required int value, int min = 0, int max = 10}) {
//   return min <= value && value <= max;

//Loop reread before class

  var sum = 11;
  while (sum < 10) {
    sum += 4;
    print(sum);
  }
}
